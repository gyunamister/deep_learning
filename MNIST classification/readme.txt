
I used Keras for developing neural network
Everyting is in MNIST.classifier.py, which contains importing, learning, evaluating function

type below command:
python MNIST_classifier.py

The thing is that different function should be called in order to build different model. (line #97)